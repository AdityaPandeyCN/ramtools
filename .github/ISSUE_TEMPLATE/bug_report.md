---
name: Bug report
about: A wrong result, a crash, or a build problem
---

**What I ran**

```
the exact command
```

**What it printed**

```
output, including anything on standard error
```

**What I expected**

For a wrong count or a missing record, the samtools command that gives the
expected answer and its output.

**Input**

SAM or BAM, coordinate sorted or not, roughly how many records, reference
naming (`chr1` or `1`). A few SAM lines that reproduce the problem, if you can.

**Environment**

- ROOT version (`root --version`):
- OS:
- RAMTools commit or release:
